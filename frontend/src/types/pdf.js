/**
 * @typedef {Object} GeneratePdfRequest
 * @property {string} [title]
 * @property {string} [source_url] - shown on the PDF's cover page if present
 * @property {import("./conversation").QaSectionDTO[]} selected_sections
 */

export {};
