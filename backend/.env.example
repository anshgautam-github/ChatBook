# App
APP_ENV=development
APP_HOST=0.0.0.0
APP_PORT=8000
CORS_ORIGINS=http://localhost:5173

# ChatGPT fetching
CHATGPT_FETCH_TIMEOUT_SECONDS=30

# Rate limiting (per client IP, applies to /parse and /generate-pdf)
RATE_LIMIT_PER_MINUTE=15
